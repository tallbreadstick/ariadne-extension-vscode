/** SecretStorage key for persisted GitHub session metadata (not the raw token). */
export const SECRET_GITHUB_SESSION = 'ariadne.github.session';

/** globalState key for terms and analytics consent metadata. */
export const GL_AUTH_CONSENT = 'ariadne.github.consent';
